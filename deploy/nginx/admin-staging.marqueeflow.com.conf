# Staging admin static files — separate deploy root from production.
server {
    listen 80;
    listen [::]:80;
    server_name admin-staging.marqueeflow.com;

    root /var/www/marqueeflow-staging/admin-panel/dist;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
